pre-commit:
  commands:
    spell:
      ren: pnpm spell
    format:
      run: pnpm format
    lint:
      run: pnpm lint

commit-msg:
  commands:
    validate:
      run: npx ts-node ./.git_hooks/commitMessage.ts
