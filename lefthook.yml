pre-commit:
  commands:
    spell:
      ren: npm run spell
    format:
      run: npm run format_check
    lint:
      run: npm run lint

commit-msg:
  commands:
    validate:
      run: npx ts-node ./.git_hooks/commitMessage.ts
