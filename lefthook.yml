pre-commit:
  commands:
    spell:
      ren: pnpm spell
    format:
      run: pnpm format_check
    lint:
      run: pnpm lint

commit-msg:
  commands:
    validate:
      run: npx ts-node ./.git_hooks/commitMessage.ts
